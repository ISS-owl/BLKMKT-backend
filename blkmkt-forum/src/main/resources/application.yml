server:
    port: 13000

spring:
    application:
        name: blkmkt-forum
    datasource:
        username: root
        password: root
        url: jdbc:mysql://39.101.64.188:3306/blkmkt_forum?useUnicode=true&characterEncoding=utf8
        driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
    mapper-locations: classpath:/mapper/**/*.xml
    global-config:
        db-config:
            id-type: auto
